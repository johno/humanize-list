'use strict';

var assert = require('assert');
var humanizeList = require('..');

describe('humanize-list', function() {

  it('should do something awesome', function() {
    assert.equal(humanizeList(), true);
  });
});
